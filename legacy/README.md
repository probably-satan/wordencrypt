# Legacy HTML Obfuscator

This folder contains the older HTML-based obfuscator (`web_app.py`, `text_obfuscator_html.py`, and sample files) kept for reference.

It is not the actively deployed app. The live/deploy entrypoint for this repository is `app.py` (Doc Defender).
