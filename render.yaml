services:
  - type: web
    name: anti-scrape-text-obfuscator
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn web_app:app --bind 0.0.0.0:$PORT
    autoDeploy: true
